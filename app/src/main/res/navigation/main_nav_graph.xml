<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_nav_graph"
    app:startDestination="@+id/home_nav_graph">

    <include android:id="@+id/favourite_nav_graph"
        app:graph="@navigation/favourite_nav_graph" />

    <include android:id="@+id/notification_nav_graph"
        app:graph="@navigation/notification_nav_graph" />

    <include android:id="@+id/profile_nav_graph"
        app:graph="@navigation/profile_nav_graph" />

    <include android:id="@+id/home_nav_graph"
        app:graph="@navigation/home_nav_graph" />
    <fragment
        android:id="@+id/fragment_auth"
        android:name="com.company.metrix.FragmentAuth"
        android:label="FragmentAuth" />
</navigation>